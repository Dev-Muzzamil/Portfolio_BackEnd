# Portfolio Website v1

## Overview
Earliest implementation with separate backend and frontend. Includes admin panel and API endpoints plus a Tailwind-based SPA. Historic extras live in `frontend/fr_old` and `frontend/extra` (not part of the tracked snapshot here).

## Backend
- Base commit: 59b5f53 initial admin/API scaffolding.
- Updates: 744b38f new controllers/routes/utilities; e44ce56 package-lock regen for deploy stability; 7212ed0 removes corrupt seed asset and simplifies seed script (tagged v1 here).
- Branches: `master`, `agent/integration-fix`; detached HEAD in working copy but history is clean at 7212ed0.

## Frontend
- Git present (`master` at 87eb4ee tagged v1).
- Tech: CRA + Tailwind, admin dashboard + portfolio sections.
- Additional archives: `fr_old` (older CRA build) and `extra` (deploy/backup artifacts, Cloudflare configs, backups).

## Status
Frozen as the earliest historic state (v1) and used as the base commit in the consolidated timeline.

---
