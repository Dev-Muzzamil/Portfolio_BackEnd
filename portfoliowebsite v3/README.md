# Portfolio Website v3

## Overview
Latest and current line. Adds further SEO, loading, and UX optimizations on both backend and frontend. This state is used for master/v3/v4 in the consolidated repo.

## Backend
- History highlights: 64047e9 module optimisations → b149d69 fix email sender → e41ae0e add sender/replyTo → 5c8ce73 server optimizations and fixes → 80aefa5 SEO/robots updates (tagged v3/v4 here).
- Branches: `main`, `heroku/master`, `origin/main` equivalents; working tree clean.

## Frontend
- History highlights: b4cacdb small SEO/title updates → 0a6fcf4 SEO/theme updates → 40a34c2 www→apex redirect → 098b360 SEO/meta/robots/sitemap tweaks (tag v1.5) → cd0e3f4 LazyImage + loading optimizations (tag v3/v4/current).
- Branches: `main`, `backup-master`; working tree clean.

## Status
Frozen as latest historic state (v3) and promoted to `master` in the consolidated timeline; `v4` currently matches this commit as a placeholder.

---
